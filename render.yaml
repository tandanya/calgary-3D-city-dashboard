services:
  - type: web
    name: calgary-dashboard-api
    env: python
    buildCommand: "cd backend && pip install -r requirements.txt"
    startCommand: "cd backend && gunicorn app:app"
    envVars:
      - key: HUGGINGFACE_API_KEY
        sync: false